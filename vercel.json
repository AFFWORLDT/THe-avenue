{
  "installCommand": "corepack enable && corepack prepare pnpm@10.0.0 --activate && pnpm install --no-frozen-lockfile --network-timeout 60000",
  "buildCommand": "pnpm build",
  "framework": "nextjs"
}
